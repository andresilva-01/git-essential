git --version = verificar a versão
git init = inicia o repositorio
git status = verifica o status do repositorio
git add = adiciona o arquivo modificado para o commit. ex: git commit -m "adicionando hello"
git commit = git commit -m "mensagem"
git push =
git pull =
git clone =
git checkout -b = cria nova branch
git checkout =
git stash = 
git stash apply = 
git branch = verifica a branch que está
git ignore = 

####OBS.: segue o fluxo para criar repositorio local####
git init
git config user.name "andresilva-01"
git config user.email "guerreiroaws01@gmail.com"
git add hello.txt
git commit -m "adicionando hello"

####OBS.: segue os passo para subir o repositorio git da maquina (remoto)
git remote add origin https://github.com/andresilva-01/git_essentials.git
git branch -M main
git push -u origin main



criar arquivo = touch <arquivo>
https://github.com/henrylle/bia
